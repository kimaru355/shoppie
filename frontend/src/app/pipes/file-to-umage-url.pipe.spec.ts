import { FileToUmageUrlPipe } from './file-to-umage-url.pipe';

describe('FileToUmageUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new FileToUmageUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
